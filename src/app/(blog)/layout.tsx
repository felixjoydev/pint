/**
 * Blog Root Layout
 *
 * Passthrough layout for public blog routes.
 * The actual layout is in [subdomain]/layout.tsx
 */

export default function BlogLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
